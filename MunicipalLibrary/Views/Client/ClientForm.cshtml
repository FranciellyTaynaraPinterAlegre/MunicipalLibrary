@model MunicipalLibrary.ViewModels.ClientFormViewModel
    ViewBag.Title = "ClientForm";
}

<h2>@ViewBag.Title</h2>

@using (Html.BeginForm("Save", "Clients"))
{
    @Html.AntiForgeryToken()
    @Html.HiddenFor(m => m.Client.Id)
    <div class="form-group">
        @Html.LabelFor(m => m.Client.Name)
        @Html.TextBoxFor(m => m.Client.Name, new { @class = "form-control" })
        @Html.ValidationMessageFor(m => m.Client.Name) 
-    </div>
    <div class="form-group">
        @Html.LabelFor(m => m.Client.BirthDate)
        @Html.TextBoxFor(m => m.Client.BirthDate, "{0: dd/MM/yyyy }", new { @class = "form-control" })
        @Html.ValidationMessageFor(m => m.Client.BirthDate)
    </div>
    <div class="form-group">
        @Html.LabelFor(m => m.Client.CivilState)
        @Html.DropDownListFor(m => m.Client.CivilState,
            new SelectList(Model.CivilState, "CivilState"),
            "Escolha", new { @class = "form-control" })
        @Html.ValidationMessageFor(m => m.CivilState)
    </div>
    <div class="form-group">
        @Html.LabelFor(m => m.Client.Sex)
        @Html.DropDownListFor(m => m.Client.Sex,
            new SelectList(Model.Sex, "CivilState"),
            "Escolha", new { @class = "form-control" })
        @Html.ValidationMessageFor(m => m.Sex)
    </div>

    <button type="submit" class="btn btn-primary">Salvar</button>
}

@section scripts
{
    @Scripts.Render("~/bundles/jqueryval")
}
